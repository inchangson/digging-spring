// 공통 Java 17, Spring Boot BOM, JUnit 설정은 루트에서 적용합니다.
// 실행 가능한 Boot 앱이 필요하면 아래 플러그인과 main 클래스를 추가합니다.
// plugins {
//     id 'org.springframework.boot'
// }

dependencies {
    implementation 'org.springframework:spring-context'
    // 이 주제에 필요한 의존성을 추가합니다.
}
